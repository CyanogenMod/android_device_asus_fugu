# Dummy definition to allow AOSP based compilation
# Those threee entries are mandatory, so filling them only to have compilation succeed
/dev/block/platform/intel/by-name/boot       /boot       emmc      defaults        defaults
/dev/block/platform/intel/by-name/system     /system     emmc      defaults        defaults
/dev/block/platform/intel/by-name/recovery   /recovery   emmc      defaults        defaults

